---
- hosts: all
  gather_facts: no
  connection: local

  tasks:

    - name: change user prompt
      eos_template:
        provider: "{{provide}}"
        src: prompt.j2
      with_items:
        - "{{prompt_name|default([])}}"
